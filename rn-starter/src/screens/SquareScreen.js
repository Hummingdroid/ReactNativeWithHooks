import React, { useState } from 'react';
import { View, StyleSheet, Button, Text } from 'react-native';

const SquareScreen = () => {
	return (
	<View>
	<Text> I am a Square Screen </Text>
	</View>
	);
};

const style = StyleSheet.create({});

export default SquareScreen;